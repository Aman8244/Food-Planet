import React from 'react'

const VegClassifier = ({ height, width }) => {
    return (
        <img width={`${width}`} height={`${height}`} src="https://img.icons8.com/color/48/vegetarian-food-symbol.png" alt="vegetarian-food-symbol" />
    )
}

export default VegClassifier
