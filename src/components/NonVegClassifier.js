import React from 'react'

const NonVegClassifier = ({height,width}) => {
    return (
        <img width={`${width}`} height={`${height}`} src="https://img.icons8.com/color/48/non-vegetarian-food-symbol.png" alt="non-vegetarian-food-symbol" />
    )
}

export default NonVegClassifier
